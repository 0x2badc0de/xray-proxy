services:
  xray-proxy:
    image: ghcr.io/0x2badc0de/xray-proxy:latest
    ports:
      - "127.0.0.1:1080:1080"
      - "127.0.0.1:8080:8080"
    environment:
      VPN_URI: "vless://..."
      SOCKS_PORT: "1080"
      HTTP_PORT: "8080"
      XRAY_LOGLEVEL: "none"
    restart: unless-stopped
