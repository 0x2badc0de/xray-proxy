services:
  xray-proxy:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1080:1080"
      - "8080:8080"
    environment:
      VPN_URI: "vless://..."
      SOCKS_PORT: "1080"
      HTTP_PORT: "8080"
      XRAY_LOGLEVEL: "none"
    restart: unless-stopped
